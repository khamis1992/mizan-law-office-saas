# ملاحظات تكامل Supabase

تم التحقق من أن مشروع Supabase المستهدف هو `mrpdsqbgmlekupjzyswx` وأنه يوفّر مفتاحاً عاماً للواجهة ومفتاح خدمة سرياً للخادم. يعتمد التطبيق المفتاح العام فقط في المتصفح، وتظل كل البيانات محكومة بسياسات RLS. لا يُحفظ مفتاح الخدمة ولا يُستخدم في الواجهة.

يوجد مخطط البيانات الخاص بالنظام في `supabase/migrations/20260814_001_law_office_reset.sql` وقد تم تطبيقه على المشروع بعد إعادة ضبط مخطط `public` بناءً على طلب المستخدم.
